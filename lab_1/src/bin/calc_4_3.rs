fn main() {
    // This following line will make the compiler complain due to syntax error in the format string.
    // Commented so that the compiler can compile the program.
    // print!("{} + {} = {", 34, 80, 80 + 34);
}
